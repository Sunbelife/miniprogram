<view class="container">

    <block wx:if="isReady">
        <tpl1
                pages="{{tplInfo.pages}}"
                bgMusic="{{tplInfo.bgMusic}}"
                invitationInfo="{{tplInfo.invitationInfo}}"
        ></tpl1>
    </block>

</view>
